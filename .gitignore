node_modules
.next
.env
.env.local
*.log
deploy-tokenbuild.tar.gz
backups

# Staging tree for the prebuilt runtime image — see scripts/pack-prebuilt.sh.
# Regenerated from .next on every package, never committed.
build

# Hardhat
artifacts
cache
dist-hardhat
typechain-types
coverage
coverage.json
*.tsbuildinfo
