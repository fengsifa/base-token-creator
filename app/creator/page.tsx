import { CreatorForm } from "../../components/creator-form";

export const metadata = {
  title: "Create an ERC-20 token · Tokenbase",
  description: "Create a standard ERC-20 token on Base Sepolia with your own wallet.",
};

export default function CreatorPage() {
  return <CreatorForm />;
}
